# Vibe Coding

## Explanation

Vibe Coding คือแนวทางการพัฒนาซอฟต์แวร์ที่ใช้ AI เป็นคู่คิดตลอดกระบวนการ ตั้งแต่ทำความเข้าใจ requirement วางแผน แก้ไขโค้ด ตรวจสอบผลลัพธ์ และปรับปรุงงาน โดยผู้พัฒนายังคงเป็นผู้ตัดสินใจ ตรวจสอบความถูกต้อง และรับผิดชอบคุณภาพของโค้ด

## Summary

โปรเจกต์ Portfolio นี้พัฒนาด้วย React และ Vite โดยใช้ AI ช่วยสำรวจโครงสร้างโค้ด ออกแบบเนื้อหา ปรับปรุง UI และตรวจสอบปัญหาจากหลักฐานจริง การทำงานแบ่งเป็นขั้นตอนดังนี้

### 1. Tool

- VS Code สำหรับพัฒนาและตรวจไฟล์
- GitHub Copilot Chat สำหรับวิเคราะห์ requirement และช่วยเสนอแนวทางแก้ไข
- React สำหรับสร้าง component และหน้า Portfolio
- Vite สำหรับ development server และ production build
- Tailwind CSS สำหรับจัด layout และ utility styles
- ESLint สำหรับตรวจสอบคุณภาพโค้ด
- GitHub สำหรับจัดเก็บและเผยแพร่ source code
- Vercel User to Public Website

### 2. Prompt

ตัวอย่าง prompt ที่ใช้ในการทำงาน:

> ช่วยวิเคราะห์โจทย์นี้ แบ่งเป็นงานย่อยที่ทดสอบได้ และบอกไฟล์ที่ควรแก้ก่อนเริ่มเขียนโค้ด

> แก้เฉพาะ component นี้ รักษา style เดิม เพิ่ม responsive layout 

> หลังแก้แล้ว ช่วยตรวจ compile error, responsive behavior และปุ่ม/ลิงก์ที่กดได้จริง พร้อมสรุปสิ่งที่ยังเสี่ยง

การเขียน prompt จะระบุไฟล์หรือ component ที่ต้องแก้ ข้อจำกัดของงาน และผลลัพธ์ที่ต้องการให้ตรวจสอบได้

### 3. Iteration

การพัฒนาไม่ได้จบในคำสั่งเดียว แต่ทำเป็นรอบสั้น ๆ:

1. อ่าน requirement และค้นหา code path ที่เกี่ยวข้อง
2. แก้ไขเฉพาะส่วนที่จำเป็น
3. ตรวจ diff และทดสอบการทำงาน
4. อ่านผลลัพธ์จาก build หรือ diagnostics
5. ปรับ prompt หรือแก้โค้ดจากหลักฐานที่พบ
6. ทำซ้ำจนได้ผลลัพธ์ที่ถูกต้องและใช้งานได้จริง

ตัวอย่างใน Portfolio คือการปรับการ์ดโปรเจกต์จากการแสดงเพียง feature และ tech stack ให้แสดง Problem, Solution, Your Role, Tech, Proof และ Outcome อย่างเป็นโครงสร้าง

### 4. Problem

ปัญหาที่พบระหว่างการพัฒนา ได้แก่:

- ปุ่ม Resume ยังลิงก์ไปยังส่วน Contact แทนที่จะดาวน์โหลด CV โดยตรง
- ข้อมูลใน Project Card ยังไม่แยกเป็น Problem, Solution และบทบาทของผู้พัฒนา
- ส่วน DocLibrary ยังไม่มี Proof และ Outcome ที่อธิบายผลงานและสิ่งที่เรียนรู้
- ต้องรักษา responsive layout และ style เดิมขณะเพิ่มข้อมูลใหม่
- ESLint แจ้งตัวแปร `formSent` และฟังก์ชัน `handleSubmit` ที่ยังไม่ได้ถูกใช้งาน

### 5. Fix

แนวทางแก้ไขที่ใช้:

- เปลี่ยน Resume link ให้ดาวน์โหลด `CV Thanawat.jpg` จาก `public/` โดยตรง
- เพิ่มข้อมูล `problem`, `solution` และ `role` ใน project data
- เพิ่ม `proof` และ `outcome` ให้กับ DocLibrary
- ปรับ Project Card ให้แสดงหัวข้อ Problem, Solution, Your Role, Tech, Proof และ Outcome
- เปลี่ยนปุ่ม Repository ให้สื่อความหมายเป็น `GitHub / Demo` และเปิดลิงก์ในแท็บใหม่
- ใช้ `npm run build` ตรวจว่า JSX และ production output ทำงานได้หลังการแก้ไข

### 6. Reflection

การใช้ AI ช่วยให้เริ่มงานได้เร็วขึ้นและมองเห็นโครงสร้างของปัญหาได้ชัดเจน แต่ AI ไม่ควรถูกใช้แทนการตรวจสอบของผู้พัฒนา ทุกการแก้ไขควรมีขอบเขตชัดเจน ตรวจสอบได้ และทดสอบด้วยหลักฐาน เช่น build, lint, diagnostics หรือการทดลองกดใช้งานจริง

สิ่งสำคัญที่ได้เรียนรู้คือ prompt ที่ดีควรบอกบริบท ไฟล์ที่เกี่ยวข้อง ข้อจำกัด และเกณฑ์การตรวจสอบ ยิ่งโจทย์ชัด การแก้ไขก็ยิ่งเล็ก ติดตามง่าย และลดผลกระทบต่อส่วนอื่นของโปรเจกต์
